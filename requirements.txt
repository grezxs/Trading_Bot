# Core (required for mock + testnet)
numpy>=1.24
pandas>=2.0
ccxt>=4.0
pyyaml>=6.0

# Regime upgrade (HMM + entropy) — TODO
hmmlearn>=0.3
scipy>=1.10

# Optional API layer
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0

# Dashboard
streamlit>=1.30
plotly>=5.18
redis>=5.0

# Dev / test
pytest>=7.0
